# ==========================================
# TechBuddy MVP - .gitignore
# ==========================================

# ============ 프로젝트 문서 (민감정보 포함 가능) ============
# docs/ 폴더는 gitignore에서 제외하여 Git에 포함시킴
# 나중에 민감정보 생기면 개별 파일 추가

# ============ Node.js ============
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# ============ Package Manager ============
package-lock.json
yarn.lock
pnpm-lock.yaml

# ============ Environment Variables ============
.env
.env.local
.env.*.local
.env.development
.env.production
.env.test

# ============ Backend (NestJS) ============
backend/dist/
backend/build/
backend/.env
backend/.env.local

# Prisma
# migrations 폴더와 schema.prisma는 Git에 포함되어야 함
# .env 파일만 제외됨 (위에서 이미 설정됨)

# ============ Frontend (Nuxt.js) ============
frontend/.nuxt/
frontend/.output/
frontend/dist/
frontend/.cache/
frontend/.env
frontend/.env.local

# ============ IDE & Editors ============
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
.clinerules
.playwright-mcp

# ============ OS ============
Thumbs.db
Desktop.ini

# ============ Logs ============
logs/
*.log

# ============ Testing ============
coverage/
.nyc_output/
*.lcov

# ============ Temporary Files ============
tmp/
temp/
*.tmp

# ============ Build Artifacts ============
*.tgz
*.zip
