# FLOWIT 페이지 구조 가이드

## 개요

Nuxt 3 기반 FLOWIT 프론트엔드의 **페이지 및 레이아웃 구조**입니다.
v1.4에서 URL 구조와 레이아웃이 변경되었습니다.

---

## 페이지 구조 변경 이력

| 버전 | URL 구조 | 레이아웃 |
|------|----------|----------|
| v1.3 | `/app/*` 기반 | 사이드바 레이아웃 |
| **v1.4** | **루트 기반** | **상단 네비 레이아웃** |

---

## 페이지 구조 (pages/)

```
frontend/
├── pages/
│   ├── index.vue                    # 메인 = 커뮤니티 홈 (/)
│   │
│   ├── auth/                        # 인증 관련
│   │   ├── login.vue                # 로그인 (/auth/login)
│   │   ├── signup.vue               # 회원가입 (/auth/signup)
│   │   └── callback.vue             # OAuth 콜백 (/auth/callback)
│   │
│   ├── community/                   # 커뮤니티 게시판
│   │   ├── index.vue                # 게시글 목록 (/community)
│   │   ├── [id].vue                 # 게시글 상세 (/community/:id)
│   │   └── write.vue                # 게시글 작성 (/community/write)
│   │
│   ├── qna/                         # Q&A
│   │   ├── index.vue                # Q&A 목록 (/qna)
│   │   ├── [id].vue                 # Q&A 상세 (/qna/:id)
│   │   └── write.vue                # Q&A 작성 (/qna/write)
│   │
│   ├── recruit/                     # 팀원 모집
│   │   ├── index.vue                # 모집글 목록 (/recruit)
│   │   ├── [id].vue                 # 모집글 상세 (/recruit/:id)
│   │   └── write.vue                # 모집글 작성 (/recruit/write)
│   │
│   ├── platform/                    # 플랫폼 (서비스 소개)
│   │   └── index.vue                # 플랫폼 메인 (/platform)
│   │
│   ├── mypage/                      # 마이페이지
│   │   ├── index.vue                # 마이페이지 홈 (/mypage)
│   │   ├── profile.vue              # 프로필 설정 (/mypage/profile)
│   │   ├── posts.vue                # 내 게시글 (/mypage/posts)
│   │   └── bookmarks.vue            # 북마크 (/mypage/bookmarks)
│   │
│   └── projects/                    # 프로젝트 관리 (새 탭)
│       ├── index.vue                # 프로젝트 목록 (/projects)
│       ├── new.vue                  # 프로젝트 생성 (/projects/new)
│       └── [id]/                    # 프로젝트 상세
│           ├── index.vue            # 칸반 보드 (/projects/:id)
│           ├── settings.vue         # 프로젝트 설정 (/projects/:id/settings)
│           └── members.vue          # 멤버 관리 (/projects/:id/members)
```

---

## 레이아웃 구조 (layouts/)

```
frontend/
├── layouts/
│   ├── default.vue          # 기본 레이아웃 (상단 네비 + 푸터)
│   ├── auth.vue             # 인증 페이지 레이아웃 (중앙 정렬)
│   └── project.vue          # 프로젝트 레이아웃 (사이드바)
```

### 1. default.vue (기본 레이아웃) ⭐ 메인

```
┌─────────────────────────────────────────────────────────────────────┐
│  [네비게이션 바]                                                     │
│  Logo | 플랫폼 | 커뮤니티 | Q&A | 팀원 모집 | 검색 | 로그인          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│                         <slot />                                    │
│                      (페이지 컨텐츠)                                  │
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│  [푸터]                                                             │
└─────────────────────────────────────────────────────────────────────┘
```

**적용 페이지**: 메인, 커뮤니티, Q&A, 팀원 모집, 마이페이지

### 2. auth.vue (인증 레이아웃)

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│                         ┌─────────────┐                            │
│                         │   <slot />  │                            │
│                         │ (폼 컨텐츠) │                            │
│                         └─────────────┘                            │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

**적용 페이지**: 로그인, 회원가입

### 3. project.vue (프로젝트 레이아웃)

```
┌────────────────┬────────────────────────────────────────────────────┐
│                │  [상단 바]                                          │
│   [Sidebar]    │  프로젝트명 | 설정 | 멤버                           │
│                ├────────────────────────────────────────────────────┤
│   📊 보드      │                                                    │
│                │                                                    │
│   📁 파일      │                    <slot />                        │
│                │                 (페이지 컨텐츠)                     │
│   👥 멤버      │                                                    │
│                │                                                    │
│   ⚙️ 설정     │                                                    │
│                │                                                    │
└────────────────┴────────────────────────────────────────────────────┘
```

**적용 페이지**: 프로젝트 상세, 칸반보드, 프로젝트 설정

---

## 라우트 매핑 정리

| 경로 | 파일 | 레이아웃 | 인증 | 설명 |
|------|------|----------|------|------|
| `/` | `pages/index.vue` | default | X | 메인 (커뮤니티 홈) |
| `/auth/login` | `pages/auth/login.vue` | auth | X | 로그인 |
| `/auth/signup` | `pages/auth/signup.vue` | auth | X | 회원가입 |
| `/auth/callback` | `pages/auth/callback.vue` | - | X | OAuth 콜백 |
| `/community` | `pages/community/index.vue` | default | X | 커뮤니티 목록 |
| `/community/:id` | `pages/community/[id].vue` | default | X | 게시글 상세 |
| `/community/write` | `pages/community/write.vue` | default | **O** | 게시글 작성 |
| `/qna` | `pages/qna/index.vue` | default | X | Q&A 목록 |
| `/qna/:id` | `pages/qna/[id].vue` | default | X | Q&A 상세 |
| `/qna/write` | `pages/qna/write.vue` | default | **O** | Q&A 작성 |
| `/recruit` | `pages/recruit/index.vue` | default | X | 모집 목록 |
| `/recruit/:id` | `pages/recruit/[id].vue` | default | X | 모집글 상세 |
| `/recruit/write` | `pages/recruit/write.vue` | default | **O** | 모집글 작성 |
| `/mypage` | `pages/mypage/index.vue` | default | **O** | 마이페이지 |
| `/mypage/profile` | `pages/mypage/profile.vue` | default | **O** | 프로필 설정 |
| `/projects` | `pages/projects/index.vue` | default | **O** | 프로젝트 목록 |
| `/projects/new` | `pages/projects/new.vue` | default | **O** | 프로젝트 생성 |
| `/projects/:id` | `pages/projects/[id]/index.vue` | project | **O** | 칸반 보드 |

---

## 레이아웃 적용 방법

```vue
<!-- pages/community/index.vue -->
<script setup lang="ts">
// default 레이아웃은 명시하지 않아도 됨
</script>

<template>
  <div>
    <!-- 커뮤니티 목록 -->
  </div>
</template>
```

```vue
<!-- pages/auth/login.vue -->
<script setup lang="ts">
definePageMeta({
  layout: 'auth'
})
</script>

<template>
  <div>
    <!-- 로그인 폼 -->
  </div>
</template>
```

```vue
<!-- pages/projects/[id]/index.vue -->
<script setup lang="ts">
definePageMeta({
  layout: 'project',
  middleware: 'auth'
})
</script>

<template>
  <div>
    <!-- 칸반 보드 -->
  </div>
</template>
```

---

## 미들웨어 (middleware/)

```
frontend/
├── middleware/
│   ├── auth.ts              # 인증 확인 미들웨어
│   └── guest.ts             # 비로그인 전용 (로그인 페이지 등)
```

### auth.ts

```typescript
// middleware/auth.ts
export default defineNuxtRouteMiddleware((to, from) => {
  const authStore = useAuthStore()

  if (!authStore.isAuthenticated) {
    // 로그인 필요 시 리다이렉트
    return navigateTo('/auth/login', {
      query: { redirect: to.fullPath }
    })
  }
})
```

### guest.ts

```typescript
// middleware/guest.ts
export default defineNuxtRouteMiddleware((to, from) => {
  const authStore = useAuthStore()

  if (authStore.isAuthenticated) {
    // 이미 로그인된 경우 메인으로
    return navigateTo('/')
  }
})
```

---

## 메인 페이지 섹션 구조

```vue
<!-- pages/index.vue -->
<template>
  <div class="home">
    <!-- 소개 배너 -->
    <section class="banner">
      <HomeBanner />
    </section>

    <!-- 실시간 인기글 -->
    <section class="popular-posts">
      <h2>실시간 인기글</h2>
      <PostList :posts="popularPosts" />
    </section>

    <!-- 실시간 인기 Q&A -->
    <section class="popular-qna">
      <h2>실시간 인기 Q&A</h2>
      <QnaList :questions="popularQna" />
    </section>

    <!-- 새로운 인사이트 -->
    <section class="insights">
      <h2>새로운 인사이트를 얻어가세요</h2>
      <InsightTabs />
    </section>

    <!-- 답변 대기 Q&A -->
    <section class="waiting-answers">
      <h2>현재 답변을 기다리고 있어요</h2>
      <QnaList :questions="waitingQna" />
    </section>

    <!-- 프로젝트 배너 -->
    <section class="project-banner">
      <ProjectBanner />
    </section>

    <!-- 팀원 모집 -->
    <section class="recruit">
      <h2>지금, 함께 할 개발자를 찾고 있어요</h2>
      <RecruitCards :recruits="latestRecruits" />
    </section>
  </div>
</template>
```

---

## 참고

- 기획서: `docs/01-planning/FLOWIT_기획서_v1_4.md`
- 서비스 플로우: `docs/06-ui-ux/01-service-flow.md`
- 네비게이션: `docs/06-ui-ux/02-navigation-structure.md`

---

*최종 업데이트: 2025.01.02*
