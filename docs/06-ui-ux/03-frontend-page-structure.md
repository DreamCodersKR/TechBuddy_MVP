# TechBuddy 프론트엔드 페이지 구조 가이드

## 개요

Nuxt 3 기반 TechBuddy 프론트엔드의 **페이지 및 레이아웃 구조**입니다.
박지승님 프론트엔드 개발 시 참고용.

---

## 앱 플로우

```
[랜딩 페이지] → [시작하기 클릭] → [로그인/회원가입] → [앱 메인 (커뮤니티)]
                                                              │
                                                    ┌─────────┴─────────┐
                                                    ▼                   ▼
                                              [커뮤니티]           [프로젝트 관리]
```

- **랜딩 페이지**: 비로그인 상태, 서비스 소개
- **앱 메인**: 로그인 후 진입, 커뮤니티가 메인 화면
- **프로젝트 관리**: 사이드바에서 접근

---

## 페이지 구조 (pages/)

```
frontend/
├── pages/
│   ├── index.vue                    # 랜딩 페이지 (/)
│   │
│   ├── auth/                        # 인증 관련
│   │   ├── login.vue                # 로그인 (/auth/login)
│   │   ├── signup.vue               # 회원가입 (/auth/signup)
│   │   └── callback.vue             # OAuth 콜백 (/auth/callback)
│   │
│   ├── app/                         # 앱 메인 (로그인 필요)
│   │   ├── index.vue                # 앱 홈 = 커뮤니티 (/app)
│   │   │
│   │   ├── community/               # 커뮤니티 (Phase 1)
│   │   │   ├── index.vue            # 게시글 목록 (/app/community)
│   │   │   ├── [id].vue             # 게시글 상세 (/app/community/:id)
│   │   │   └── write.vue            # 게시글 작성 (/app/community/write)
│   │   │
│   │   ├── projects/                # 프로젝트 관리 (Phase 2)
│   │   │   ├── index.vue            # 프로젝트 목록 (/app/projects)
│   │   │   ├── new.vue              # 프로젝트 생성 (/app/projects/new)
│   │   │   └── [id]/                # 프로젝트 상세
│   │   │       ├── index.vue        # 칸반 보드 (/app/projects/:id)
│   │   │       ├── settings.vue     # 프로젝트 설정 (/app/projects/:id/settings)
│   │   │       └── members.vue      # 멤버 관리 (/app/projects/:id/members)
│   │   │
│   │   ├── mentoring/               # 멘토링 (Phase 3 - Coming Soon)
│   │   │   └── index.vue            # 준비중 페이지
│   │   │
│   │   └── settings/                # 사용자 설정
│   │       └── index.vue            # 프로필 설정 (/app/settings)
│   │
│   └── about.vue                    # 소개 페이지 (/about)
```

---

## 레이아웃 구조 (layouts/)

```
frontend/
├── layouts/
│   ├── default.vue          # 기본 레이아웃 (랜딩 페이지용)
│   ├── auth.vue              # 인증 페이지 레이아웃 (중앙 정렬)
│   └── app.vue               # 앱 레이아웃 (사이드바 + 메인)
```

### 1. default.vue (랜딩 페이지용)

```
┌─────────────────────────────────────────────────────────────────────┐
│  [Header]                                                           │
│  Logo | 커뮤니티 | 프로젝트 | 멘토링(Coming Soon) | 소개 | [시작하기] │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│                         <slot />                                    │
│                      (페이지 컨텐츠)                                  │
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│  [Footer]                                                           │
└─────────────────────────────────────────────────────────────────────┘
```

### 2. auth.vue (로그인/회원가입용)

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│                         ┌─────────────┐                            │
│                         │   <slot />  │                            │
│                         │ (폼 컨텐츠) │                            │
│                         └─────────────┘                            │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 3. app.vue (앱 메인 - 사이드바 레이아웃) ⭐ 중요

```
┌────────────────┬────────────────────────────────────────────────────┐
│                │  [Top Bar]                                         │
│   [Sidebar]    │  검색 | 알림 | 프로필                               │
│                ├────────────────────────────────────────────────────┤
│   🏠 홈        │                                                    │
│   (커뮤니티)    │                                                    │
│                │                    <slot />                        │
│   📁 프로젝트   │                 (페이지 컨텐츠)                     │
│                │                                                    │
│   👤 멘토링    │                                                    │
│   (Coming Soon)│                                                    │
│                │                                                    │
│   ──────────── │                                                    │
│   ⚙️ 설정     │                                                    │
│                │                                                    │
└────────────────┴────────────────────────────────────────────────────┘
```

---

## 라우트 매핑 정리

| 경로                         | 파일                                   | 레이아웃 | 설명             |
| ---------------------------- | -------------------------------------- | -------- | ---------------- |
| `/`                          | `pages/index.vue`                      | default  | 랜딩 페이지      |
| `/about`                     | `pages/about.vue`                      | default  | 소개 페이지      |
| `/auth/login`                | `pages/auth/login.vue`                 | auth     | 로그인           |
| `/auth/signup`               | `pages/auth/signup.vue`                | auth     | 회원가입         |
| `/app`                       | `pages/app/index.vue`                  | app      | 앱 홈 (커뮤니티) |
| `/app/community`             | `pages/app/community/index.vue`        | app      | 게시글 목록      |
| `/app/community/:id`         | `pages/app/community/[id].vue`         | app      | 게시글 상세      |
| `/app/projects`              | `pages/app/projects/index.vue`         | app      | 프로젝트 목록    |
| `/app/projects/new`          | `pages/app/projects/new.vue`           | app      | 프로젝트 생성    |
| `/app/projects/:id`          | `pages/app/projects/[id]/index.vue`    | app      | 칸반 보드        |
| `/app/projects/:id/settings` | `pages/app/projects/[id]/settings.vue` | app      | 프로젝트 설정    |
| `/app/projects/:id/members`  | `pages/app/projects/[id]/members.vue`  | app      | 멤버 관리        |

---

## 박지승님 담당 범위 (Phase 2 - 프로젝트 관리)

### 우선순위 순서

1. **프로젝트 목록** (`/app/projects`)

   - 내 프로젝트 목록 표시
   - 프로젝트 카드 UI (이름, 상태, 멤버 수)
   - "새 프로젝트" 버튼

2. **프로젝트 생성** (`/app/projects/new`)

   - 프로젝트명 입력
   - 설명 입력
   - 생성 버튼 → API 호출

3. **칸반 보드** (`/app/projects/:id`) ⭐ 핵심

   - 4개 컬럼: Todo | In Progress | Done | Help
   - 태스크 카드 드래그앤드롭
   - 태스크 추가/수정/삭제
   - Linear.app UI 참고

4. **멤버 관리** (`/app/projects/:id/members`)

   - 멤버 목록 (역할 표시)
   - 멤버 초대
   - 역할 변경 (PM만 가능)

5. **프로젝트 설정** (`/app/projects/:id/settings`)
   - 프로젝트 정보 수정
   - 프로젝트 삭제 (PM만)

---

## 레이아웃 적용 방법

각 페이지에서 `definePageMeta`로 레이아웃 지정:

```vue
<!-- pages/app/projects/index.vue -->
<script setup lang="ts">
definePageMeta({
  layout: "app",
  middleware: "auth", // 인증 필요
});
</script>

<template>
  <div>
    <!-- 프로젝트 목록 컨텐츠 -->
  </div>
</template>
```

```vue
<!-- pages/auth/login.vue -->
<script setup lang="ts">
definePageMeta({
  layout: "auth",
});
</script>

<template>
  <div>
    <!-- 로그인 폼 -->
  </div>
</template>
```

---

## 미들웨어 (middleware/)

```
frontend/
├── middleware/
│   └── auth.ts              # 인증 확인 미들웨어
```

```typescript
// middleware/auth.ts
export default defineNuxtRouteMiddleware((to, from) => {
  const authStore = useAuthStore();

  if (!authStore.isAuthenticated) {
    return navigateTo("/auth/login");
  }
});
```

---

## 참고

- UI 레퍼런스: [Linear.app](https://linear.app)
- 헤더 가이드: [01-landing-header.md](./01-landing-header.md)
- 컨텐츠 가이드: [02-landing-content.md](./02-landing-content.md)

---

_작성일: 2025.12.01_
